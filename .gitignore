# Runtime data — contains TLS private keys, client UUIDs, and credentials.
# NEVER commit this directory.
data/

# Local environment file contains the terminal password.
.env

# Editor / OS noise
.DS_Store
*.swp
*.swo
*~
Thumbs.db
